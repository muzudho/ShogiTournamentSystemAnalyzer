# プログラム4パート構成 第100ステップ実行計画

## 目的

最終順位行の生成入口を `FinalRankingDomain` へ寄せ、シミュレーション域から順位行ビルダーへの直接依存を減らす。

## 今回の範囲

- `FinalRankingDomain` に標準順位行と本戦順位行の生成入口を追加する。
- `StandardSimulationMainline` / `FinalStageSimulationMainline` / `TournamentFramework` シミュレーション側の順位行生成呼び出しを `FinalRankingDomain` 経由へ変更する。
- 既存の `RankingResultRowBuilder` は実装詳細として残し、計算ロジックと出力内容は変えない。

## 今回は変えないこと

- シミュレーション mainline 内で最終順位表を出力している実行順。
- 品質評価側の順位行生成。
- CSV / Markdown writer の責務分割。

## 検証

- `dotnet build .\ShogiTournamentSystemAnalyzer\ShogiTournamentSystemAnalyzer.csproj`
- `dotnet run --project .\ShogiTournamentSystemAnalyzer\ShogiTournamentSystemAnalyzer.csproj -- --request-file Inputs/Smoke/simulation_input_[先手1x後手1]_[Neutral_Exact]_[STSAInput4_smoke].request.txt`
- `dotnet run --project .\ShogiTournamentSystemAnalyzer\ShogiTournamentSystemAnalyzer.csproj -- --request-file Inputs/Smoke/simulation_input_[本戦16]_[FinalStage_Exact]_[STSAInput4_smoke].request.txt`
- `git diff --check`