# プログラム4パート構成 第43ステップ実行計画

## 目的

`Application/DataDefinitions` に残っているテーブル定義読み取りを、実際の利用先である `Analysis/Boundaries/FinalRanking/` 配下へ配置する。

現時点では FinalRanking の CSV / Markdown 出力だけがテーブル定義を参照しているため、第43ステップでは FinalRanking 境界の補助として扱う。

## 対象

- `Application/DataDefinitions/TableTypeDefinitionReader.cs`
- `Application/DataDefinitions/TabularDataDefinitionModels.cs`
- `Application/Execution/Analysis/Boundaries/FinalRanking/DataDefinitions/`

## 作業

1. `Application/Execution/Analysis/Boundaries/FinalRanking/DataDefinitions/` を作る。
2. `Application/DataDefinitions/*.cs` を移動する。
3. namespace は今回は維持し、外部挙動を変えない。
4. FinalRanking 以外で参照されていないことを確認する。

## 確認

- `dotnet build .\ShogiTournamentSystemAnalyzer\ShogiTournamentSystemAnalyzer.csproj`
- 空ルールの要求ファイル入力スモーク
- 手動入力保存のスモーク

## 今回は触らないこと

- namespace の大規模変更
- FinalRanking 出力ロジックの変更
- テーブル定義 JSON の仕様変更