# プログラム4パート構成 第52ステップ実行計画

## 目的

分析側の旧分類 namespace のうち、TournamentQualityEvaluator ドメインをフォルダー構成に合わせる。

第51ステップで Simulation ドメインを整理したため、第52ステップでは残っている `Application.Modes` を TournamentQualityEvaluator 側へ移す。

## 対象

- `Application/Analysis/Domains/TournamentQualityEvaluator/Modes/`
- `Application.Modes` を参照している `using`

## 作業

1. TournamentQualityEvaluator の Modes namespace をフォルダー構成へ合わせる。
2. `using ShogiTournamentSystemAnalyzer.Application.Modes;` を新 namespace へ更新する。
3. 挙動は変えず、namespace 整理だけに限定する。

## 確認

- `dotnet build .\ShogiTournamentSystemAnalyzer\ShogiTournamentSystemAnalyzer.csproj`
- 空ルールの要求ファイル入力スモーク
- 手動入力保存のスモーク

## 今回は触らないこと

- 境界側 namespace の全面変更
- 分析ロジックの変更
- 品質評価の計算条件変更