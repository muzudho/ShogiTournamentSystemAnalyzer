# プログラム4パート構成 第28ステップ実行計画

## 目的

ライフサイクル上の `RequestFileCheck` フォルダーへ、要求ファイルチェック境界のクラスを配置する。

第27ステップで `Analysis` を配置したため、第28ステップでは要求ファイルチェックの実体を `Application/Execution/RequestFileCheck/` に移し、全体図とコード配置をさらに対応させる。

## 対象

- `ShogiTournamentSystemAnalyzer/Application/Input/RequestFileCheck.cs`
- `ShogiTournamentSystemAnalyzer/Application/Input/RequestFileCheckResult.cs`
- `ShogiTournamentSystemAnalyzer/Application/Execution/RequestFileCheck/`

## 作業

1. 配置マップを更新する。
2. `RequestFileCheck.cs` を `Application/Execution/RequestFileCheck/` へ移動する。
3. `RequestFileCheckResult.cs` を `Application/Execution/RequestFileCheck/` へ移動する。
4. namespace は今回は維持し、外部挙動を変えない。

## 確認

- `dotnet build .\ShogiTournamentSystemAnalyzer\ShogiTournamentSystemAnalyzer.csproj`
- 空ルールの要求ファイル入力スモーク
- 手動入力保存のスモーク

## 今回は触らないこと

- `RequestInputFileReader` や STSA 変換処理の移動
- `InputSourceConfiguration` の分岐整理
- namespace の大規模変更
- 要求ファイルの入出力書式