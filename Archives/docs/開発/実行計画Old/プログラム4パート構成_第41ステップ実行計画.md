# プログラム4パート構成 第41ステップ実行計画

## 目的

`Application/Modes` に残っているシミュレーション文脈と主線実行を、`Analysis/Domains/Simulation/` 配下へ配置する。

第40ステップでトップレベルの Simulation モードを移したため、第41ステップでは残りの `SimulationContext` と `SimulationMainline` を同じシミュレーション域へ移す。

## 対象

- `Application/Modes/SimulationContext/*.cs`
- `Application/Modes/SimulationMainline/*.cs`
- `Application/Execution/Analysis/Domains/Simulation/SimulationContext/`
- `Application/Execution/Analysis/Domains/Simulation/SimulationMainline/`

## 作業

1. 配置マップを更新する。
2. `SimulationContext` フォルダーを `Analysis/Domains/Simulation/` へ移動する。
3. `SimulationMainline` フォルダーを `Analysis/Domains/Simulation/` へ移動する。
4. namespace は今回は維持し、外部挙動を変えない。

## 確認

- `dotnet build .\ShogiTournamentSystemAnalyzer\ShogiTournamentSystemAnalyzer.csproj`
- 空ルールの要求ファイル入力スモーク
- 手動入力保存のスモーク

## 今回は触らないこと

- `Application/UseCases/Simulation` の移動
- namespace の大規模変更
- シミュレーションロジックの変更