# プログラム4パート構成 第31ステップ実行計画

## 目的

ライフサイクル上の `ManualInput` フォルダーへ、手動入力ルートの入口クラスを配置する。

第31ステップでは、手動入力開始を担当する `ManualInput` を `Application/Execution/ManualInput/` に移す。

## 対象

- `ShogiTournamentSystemAnalyzer/Application/Input/ManualInput.cs`
- `ShogiTournamentSystemAnalyzer/Application/Execution/ManualInput/`

## 作業

1. 配置マップを更新する。
2. `ManualInput.cs` を `Application/Execution/ManualInput/` へ移動する。
3. namespace は今回は維持し、外部挙動を変えない。

## 確認

- `dotnet build .\ShogiTournamentSystemAnalyzer\ShogiTournamentSystemAnalyzer.csproj`
- 空ルールの要求ファイル入力スモーク
- 手動入力保存のスモーク

## 今回は触らないこと

- `RequestFileCreatePrompt` の移動
- `ManualInputRecordingSessionStarter` の移動
- namespace の大規模変更
- 要求ファイルの入出力書式