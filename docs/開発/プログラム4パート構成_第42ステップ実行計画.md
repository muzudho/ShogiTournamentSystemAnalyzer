# プログラム4パート構成 第42ステップ実行計画

## 目的

`Application/UseCases/Simulation` に残っているシミュレーション用ユースケースを、`Analysis/Domains/Simulation/` 配下へ配置する。

第40・第41ステップでシミュレーションのモード、文脈、主線実行を移したため、第42ステップではシナリオ実行のユースケースも同じシミュレーション域へ移す。

## 対象

- `Application/UseCases/Simulation/*.cs`
- `Application/Execution/Analysis/Domains/Simulation/UseCases/`

## 作業

1. 配置マップを更新する。
2. `Application/Execution/Analysis/Domains/Simulation/UseCases/` を作る。
3. `Application/UseCases/Simulation/*.cs` を移動する。
4. namespace は今回は維持し、外部挙動を変えない。
5. `.gitkeep` だけのフォルダーは今回触らない。

## 確認

- `dotnet build .\ShogiTournamentSystemAnalyzer\ShogiTournamentSystemAnalyzer.csproj`
- 空ルールの要求ファイル入力スモーク
- 手動入力保存のスモーク

## 今回は触らないこと

- `.gitkeep` だけの `Application/UseCases` サブフォルダー
- namespace の大規模変更
- シミュレーションロジックの変更