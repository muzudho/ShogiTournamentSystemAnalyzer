# プログラム4パート構成 第39ステップ実行計画

## 目的

アプリケーション起動準備を、ライフサイクル上の `BeforeRequestFileCheck` フォルダーへ配置する。

`ApplicationStartup` は、実行時間予算を開始し、プログラム導入表示を出す。これは要求ファイルチェックや手動入力に入る前の準備なので、`BeforeRequestFileCheck` に置く。

## 対象

- `ShogiTournamentSystemAnalyzer/Application/Execution/ApplicationStartup.cs`
- `ShogiTournamentSystemAnalyzer/Application/Execution/BeforeRequestFileCheck/`

## 作業

1. 配置マップを更新する。
2. `ApplicationStartup.cs` を `Application/Execution/BeforeRequestFileCheck/` へ移動する。
3. namespace は今回は維持し、外部挙動を変えない。
4. `#メモ：` や `#ここは：` で始まるユーザー用メモは触らない。

## 確認

- `dotnet build .\ShogiTournamentSystemAnalyzer\ShogiTournamentSystemAnalyzer.csproj`
- 空ルールの要求ファイル入力スモーク
- 手動入力保存のスモーク

## 今回は触らないこと

- `ApplicationRun` 系の移動
- `Shared` のメモファイル
- namespace の大規模変更
- 起動処理の仕様変更