# プログラム4パート構成 第61ステップ実行計画

## 目的

ルート `Inputs` / `Data` / `Output` 統合と第60ステップ後に残った小さな後始末を行う。

## 対象

- `ShogiTournamentSystemAnalyzer/Application/BeforeManualInput/.gitkeep`
- `ShogiTournamentSystemAnalyzer/Examples/README.md`
- `Inputs/Smoke/*_from_project_inputs.txt`
- `docs/開発/プログラム4パート構成_配置マップ.md`

## 作業

1. 実ファイルが入った `BeforeManualInput` の `.gitkeep` 削除を受け入れる。
2. `Examples/README.md` に残っていた `Data/Inputs` を `Inputs` へ更新する。
3. ルート移動時に退避名で残した旧 smoke 入力を確認し、重複する旧形式として削除する。
4. 配置マップに、第61ステップの足元整理を記録する。

## 確認

- `rg "Data/Inputs|from_project_inputs" README.md docs ShogiTournamentSystemAnalyzer Inputs Data`
- `git status --short --untracked-files=all`

## 今回は触らないこと

- 入力ファイル仕様の変更
- 品質評価 smoke 入力の内容変更
- Simulation モデルの構造整理