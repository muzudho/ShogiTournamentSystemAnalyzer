# プログラム4パート構成 第55ステップ実行計画

## 目的

主線を表すクラス名を `Workflow` 方針に揃える。

`Run` はメソッド名として残し、クラス名では「複数ステップを順番に進める主線」を表す `Workflow` を使う。

## 対象

- `Application/ApplicationRun.cs` -> `Application/ApplicationWorkflow.cs`
- `Application/ApplicationRequestRun.cs` -> `Application/RequestWorkflow.cs`
- `Application/ApplicationRequestSessionRun.cs` -> `Application/RequestSessionWorkflow.cs`
- `Application/Analysis/AnalysisRun.cs` -> `Application/Analysis/AnalysisWorkflow.cs`

## 作業

1. 主線クラスのファイル名とクラス名を `Workflow` へリネームする。
2. 呼び出し側の参照を新しいクラス名へ更新する。
3. `Run()` メソッド名は実行操作として残す。
4. 挙動は変えず、命名整理だけに限定する。

## 確認

- `dotnet build .\ShogiTournamentSystemAnalyzer\ShogiTournamentSystemAnalyzer.csproj`
- 空ルールの要求ファイル入力スモーク
- 手動入力保存のスモーク

## 今回は触らないこと

- 個別処理クラスへの `Workflow` 付与
- 分析フローの処理順序
- 4 大域 / 4 大境界の再分類