# プログラム4パート構成 第140ステップ実行計画

## 目的

`STSAInput/5` の複数ステップ要求で使っている `Step.` 階層セクション名を、`Step` 接尾辞のセクション名へ変更する。

現在の複数ステップ形式は、ステップ固有設定を `Step.Simulation` / `Step.QualityEvaluation`、属性を `Step.Simulation.RuleProfileAttributes` / `Step.QualityEvaluation.RuleProfileAttributes` としている。これは `Step` カテゴリ配下のステップインスタンスを表しているが、セクション parser は階層構造を持たず、実装上は文字列キーとして扱っている。

第140ステップでは、正規表現や配列表記を増やさず、ステップ名を先頭に出した `SimulationStep` / `QualityEvaluationStep` 形式へ寄せる。writer は新形式を出し、parser は必要に応じて旧形式を互換入力として読む。

## 新しいセクション名

複数ステップ時のステップ固有設定:

```plaintext
#[Section] SimulationStep
#[Section] QualityEvaluationStep
```

複数ステップ時の属性:

```plaintext
#[Section] SimulationStep.RuleProfileAttributes
#[Section] QualityEvaluationStep.RuleProfileAttributes
```

複数ステップ時のステップ別入力と出力:

```plaintext
#[Section] SimulationStep.PlayersCsv
#[Section] SimulationStep.GroupMapCsv
#[Section] SimulationStep.AdditionalApexPlayersCsv
#[Section] SimulationStep.MatchesInput
#[Section] SimulationStep.ReferenceMatchesInput
#[Section] SimulationStep.Inputs
#[Section] SimulationStep.Output

#[Section] QualityEvaluationStep.PlayersCsv
#[Section] QualityEvaluationStep.GroupMapCsv
#[Section] QualityEvaluationStep.AdditionalApexPlayersCsv
#[Section] QualityEvaluationStep.MatchesInput
#[Section] QualityEvaluationStep.ReferenceMatchesInput
#[Section] QualityEvaluationStep.Output
```

単一ステップ時の `RuleProfileAttributes` / `PlayersCsv` / `MatchesInput` / `Output` は現状維持する。

## 実施内容

1. `StsaInputRequestParser` にステップセクション名 helper を追加する。
   - `FormatStepName(...)` は `Simulation` / `QualityEvaluation` を返す既存仕様のまま維持する。
   - 新 helper は `SimulationStep` / `QualityEvaluationStep` を返す。
2. 複数ステップ parser の必須セクション名を新形式へ変更する。
   - `Step.Simulation` -> `SimulationStep`
   - `Step.QualityEvaluation` -> `QualityEvaluationStep`
   - `Step.*.RuleProfileAttributes` -> `*Step.RuleProfileAttributes`
3. 複数ステップの body section prefix を新形式へ変更する。
   - `Simulation.PlayersCsv` -> `SimulationStep.PlayersCsv`
   - `QualityEvaluation.Output` -> `QualityEvaluationStep.Output`
4. 互換読み取り helper を追加する。
   - primary は新形式、legacy は旧形式とする。
   - 旧形式だけ存在する要求ファイルは引き続き parse できるようにする。
   - 新旧両方が同時にある場合は、曖昧さを避けるため重複エラーにする。
5. `StsaInputRequestWriter` の `STSAInput/5` 複数ステップ出力を新形式へ変更する。
   - `STSAInput/4` writer の互換 `RuleProfileMode` 出力も、複数ステップの section prefix は新形式へ合わせるかを確認する。
   - 方針は「複数ステップ section 名の整理」であり、属性形式だけに限定しない。
6. `STSAInput/5 仕様` を更新する。
   - 複数ステップ例を `SimulationStep` / `QualityEvaluationStep` 形式へ差し替える。
   - 旧 `Step.Simulation` 形式は互換入力として読むが、writer は新形式を出す旨を追記する。
7. 既存 smoke 入力を新形式へ更新する。
   - リポジトリ内の代表 `STSAInput/5` 複数ステップ要求を新形式へ寄せる。
   - 互換読み取りを残す場合は、旧形式用の parse smoke を最小限残す。

## 変えないこと

- `AnalysisFlowSteps=Simulation,QualityEvaluation` の値。
- 単一ステップ要求の section 名。
- `RuleProfileAttributes` のキー構造と検証規則。
- 計算ロジック、入力 CSV の列、出力 CSV / Markdown の内容。
- `AnalysisRequest` / `AnalysisStepRequest` の内部構造。

## 互換方針

writer は新形式のみを出す。

parser は当面、次の旧形式を互換入力として読む。

```plaintext
#[Section] Step.Simulation
#[Section] Step.Simulation.RuleProfileAttributes
#[Section] Simulation.Output
```

ただし、同じ意味の新旧 section が同時に存在する場合は、どちらを採用するか曖昧になるためエラーにする。

## 検証

- `dotnet build .\ShogiTournamentSystemAnalyzer\ShogiTournamentSystemAnalyzer.csproj`
- `dotnet run --project .\ShogiTournamentSystemAnalyzer\ShogiTournamentSystemAnalyzer.csproj -- --smoke-request-writer-roundtrip --format STSAInput/5 --output-dir Output/SmokeGenerated/WriterStsa5 "Inputs\Smoke\analysis_input_[先手8x後手8]_[Simulation_QualityEvaluation_STSAInput5_smoke].request.txt"`
- `dotnet run --project .\ShogiTournamentSystemAnalyzer\ShogiTournamentSystemAnalyzer.csproj -- --request-file "Inputs\Smoke\analysis_input_[先手8x後手8]_[Simulation_QualityEvaluation_STSAInput5_smoke].request.txt"`
- 互換を残す場合、旧 `Step.Simulation` 形式の要求ファイルを parser smoke で確認する。
- `rg "Step\.Simulation|Step\.QualityEvaluation|Simulation\.Output|QualityEvaluation\.Output" docs Inputs ShogiTournamentSystemAnalyzer` で旧表記の残りを確認する。

## 完了条件

- `STSAInput/5` の複数ステップ writer 出力が `SimulationStep` / `QualityEvaluationStep` 形式になる。
- 新形式の writer round-trip smoke が成功する。
- 既存の代表複数ステップ要求ファイルが新形式で実行できる。
- 旧形式を互換として残す場合、旧形式の parse 成功範囲が明示されている。
- `docs/設計/STSAInput5仕様.md` の複数ステップ説明が新形式へ更新されている。

## 実施結果

未実施。

## 次ステップ候補

- `STSAInput/4` の複数ステップ section 名も同じ `*Step` 形式へ寄せるか、互換形式として旧 `Step.*` を維持するかを最終確認する。
- 旧 `Step.*` 互換入力をどの時点で廃止するかを決める。