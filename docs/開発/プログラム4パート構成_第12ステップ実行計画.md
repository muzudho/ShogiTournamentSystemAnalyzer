# プログラム4パート構成 第12ステップ実行計画

## 目的

第11ステップで品質評価向け STSA 変換を `StsaQualityEvaluationLegacyConverter` に分離した。
第12ステップでは、シミュレーション向け STSA 変換を `StsaSimulationLegacyConverter` に分離する。

## 現状

`StsaInputLegacyConverter` には、まだ次のシミュレーション向け変換が残っている。

- 大会進行フレームワーク
- 空ルール

これらを移すと、`StsaInputLegacyConverter` は STSA format の共通入口と flow / rule による dispatch 役に近づく。

## 今回やること

1. `Application/Input/StsaSimulationLegacyConverter.cs` を追加する。
2. `ConvertStsaTournamentFramework` を移す。
3. `ConvertStsaEmpty` を移す。
4. `StsaInputLegacyConverter` はシミュレーションの場合に専用 converter を呼ぶ。
5. 変換仕様や出力文字列は変えない。

## 今回やらないこと

- 通常シミュレーション標準ルールの STSA 対応追加
- 大会進行フレームワーク変換の内部整理
- 空ルール変換の内部整理
- dispatch の enum 化

## 完了条件

- シミュレーション向け STSA 変換が `StsaSimulationLegacyConverter` に分離されている。
- `StsaInputLegacyConverter` が dispatch 役に近づいている。
- `dotnet build` が成功する。
- `--request-file` 経由の空ルール smoke 実行が成功する。