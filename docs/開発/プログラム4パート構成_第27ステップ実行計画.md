# プログラム4パート構成 第27ステップ実行計画

## 目的

ライフサイクル用フォルダーへコードを配置し、フォルダー単位で進捗が見える形に切り替える。

第27ステップでは、責務が明確な `Analysis` 系の実行クラスを `Application/Execution/Analysis/` に移す。

## 対象

- `ShogiTournamentSystemAnalyzer/Application/Execution/Analysis.cs`
- `ShogiTournamentSystemAnalyzer/Application/Execution/AnalysisFlowDispatcher.cs`
- `ShogiTournamentSystemAnalyzer/Application/Execution/Analysis/`

## 作業

1. 配置マップ文書を追加する。
2. `Analysis.cs` を `Application/Execution/Analysis/` へ移動する。
3. `AnalysisFlowDispatcher.cs` を `Application/Execution/Analysis/` へ移動する。
4. namespace は今回は維持し、外部挙動を変えない。

## 確認

- `dotnet build .\ShogiTournamentSystemAnalyzer\ShogiTournamentSystemAnalyzer.csproj`
- 空ルールの要求ファイル入力スモーク
- 手動入力保存のスモーク

## 今回は触らないこと

- `Application.Input` 配下の移動
- namespace の大規模変更
- 分析フローの中身
- 要求ファイルの入出力書式