# プログラム4パート構成 第46ステップ実行計画

## 目的

`Application/Helpers` に残っている分析モード共通補助を、`Analysis/Boundaries/Request/Support/` 配下へ配置する。

`ModeSupportHelpers` は Simulation と TournamentQualityEvaluator の両方から使われるが、責務は本戦グループ対応の読み取りや、対局予定に出る選手だけへ入力データを整えることである。そのため、特定の分析ドメインではなく Request 境界の入力補助として扱う。

## 対象

- `Application/Helpers/ModeSupportHelpers.cs`
- `Application/Execution/Analysis/Boundaries/Request/Support/ModeSupportHelpers.cs`

## 作業

1. `Application/Execution/Analysis/Boundaries/Request/Support/` を作る。
2. `ModeSupportHelpers.cs` を移動する。
3. namespace は今回は維持し、呼び出し側の挙動を変えない。
4. `Application/Helpers` 直下に実コードを残さない。

## 確認

- `dotnet build .\ShogiTournamentSystemAnalyzer\ShogiTournamentSystemAnalyzer.csproj`
- 空ルールの要求ファイル入力スモーク
- 手動入力保存のスモーク

## 今回は触らないこと

- namespace の大規模変更
- 入力補助ロジックの変更
- Console 入力処理の変更