# プログラム4パート構成 第45ステップ実行計画

## 目的

`Application/Validation` に残っている本戦入力チェックを、`Analysis/Boundaries/Request/Validation/` 配下へ配置する。

`FinalStageValidators` は Simulation と TournamentQualityEvaluator の両方から使われるが、責務は本戦用の選手一覧・グループ対応・対局一覧という要求データの整合性チェックである。そのため、特定の分析ドメインではなく Request 境界の検証として扱う。

## 対象

- `Application/Validation/FinalStageValidators.cs`
- `Application/Execution/Analysis/Boundaries/Request/Validation/FinalStageValidators.cs`

## 作業

1. `Application/Execution/Analysis/Boundaries/Request/Validation/` を作る。
2. `FinalStageValidators.cs` を移動する。
3. namespace は今回は維持し、呼び出し側の挙動を変えない。
4. `Application/Validation` 直下に実コードを残さない。

## 確認

- `dotnet build .\ShogiTournamentSystemAnalyzer\ShogiTournamentSystemAnalyzer.csproj`
- 空ルールの要求ファイル入力スモーク
- 手動入力保存のスモーク

## 今回は触らないこと

- namespace の大規模変更
- 検証条件やエラーメッセージの変更
- Simulation / TournamentQualityEvaluator 側の入力フロー変更