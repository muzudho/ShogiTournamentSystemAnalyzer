# プログラム4パート構成 第34ステップ実行計画

## 目的

要求ファイルチェック時に使う読み取り・形式判定・レガシー変換系のクラスを、`Application/Execution/RequestFileCheck/` にまとめて配置する。

これにより、要求ファイルチェックの中で「読む」「形式を判定する」「STSA/レガシー入力へ変換する」処理を、ライフサイクル図の `RequestFileCheck` 節点から探しやすくする。

## 対象

- `RequestInputFileReader.cs`
- `RequestInputFormatDetector.cs`
- `LegacyInputFileFilter.cs`
- `LegacyInputLineBuilder.cs`
- `StsaInputLegacyConverter.cs`
- `StsaInputSectionParser.cs`
- `StsaInputValueParser.cs`
- `StsaQualityEvaluationLegacyConverter.cs`
- `StsaSimulationLegacyConverter.cs`
- `Application/Execution/RequestFileCheck/`

## 作業

1. 配置マップを更新する。
2. 対象ファイルを `Application/Execution/RequestFileCheck/` へ移動する。
3. namespace は今回は維持し、外部挙動を変えない。

## 確認

- `dotnet build .\ShogiTournamentSystemAnalyzer\ShogiTournamentSystemAnalyzer.csproj`
- 空ルールの要求ファイル入力スモーク
- 手動入力保存のスモーク

## 今回は触らないこと

- `InputSourceConfiguration` の移動
- `RequestInputSession` の移動
- namespace の大規模変更
- 要求ファイルの入出力書式