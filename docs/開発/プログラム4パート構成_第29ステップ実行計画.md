# プログラム4パート構成 第29ステップ実行計画

## 目的

ライフサイクル上の `BeforeRequestFileCheck` フォルダーへ、要求ファイルチェック前のクラスを配置する。

第29ステップでは、コマンドライン引数から `--input-file` 指定を読み取るクラスを `Application/Execution/BeforeRequestFileCheck/` に移す。

## 対象

- `ShogiTournamentSystemAnalyzer/Application/Input/RequestFileArgumentReader.cs`
- `ShogiTournamentSystemAnalyzer/Application/Input/RequestFileArgumentReadResult.cs`
- `ShogiTournamentSystemAnalyzer/Application/Execution/BeforeRequestFileCheck/`

## 作業

1. 配置マップを更新する。
2. `RequestFileArgumentReader.cs` を `Application/Execution/BeforeRequestFileCheck/` へ移動する。
3. `RequestFileArgumentReadResult.cs` を `Application/Execution/BeforeRequestFileCheck/` へ移動する。
4. namespace は今回は維持し、外部挙動を変えない。

## 確認

- `dotnet build .\ShogiTournamentSystemAnalyzer\ShogiTournamentSystemAnalyzer.csproj`
- 空ルールの要求ファイル入力スモーク
- 手動入力保存のスモーク

## 今回は触らないこと

- `InputSourceConfiguration` の移動
- 要求ファイルチェック本体の追加変更
- namespace の大規模変更
- 要求ファイルの入出力書式