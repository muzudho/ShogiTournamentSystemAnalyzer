# プログラム4パート構成 第32ステップ実行計画

## 目的

ライフサイクル上の `BeforeRequestFileCreate` フォルダーへ、要求ファイル作成前の確認処理を配置する。

第32ステップでは、手動入力時に入力を保存するかどうかを確認し、保存する場合の出力先を決める `RequestFileCreatePrompt` を `Application/Execution/BeforeRequestFileCreate/` に移す。

## 対象

- `ShogiTournamentSystemAnalyzer/Application/Input/RequestFileCreatePrompt.cs`
- `ShogiTournamentSystemAnalyzer/Application/Execution/BeforeRequestFileCreate/`

## 作業

1. 配置マップを更新する。
2. `RequestFileCreatePrompt.cs` を `Application/Execution/BeforeRequestFileCreate/` へ移動する。
3. namespace は今回は維持し、外部挙動を変えない。

## 確認

- `dotnet build .\ShogiTournamentSystemAnalyzer\ShogiTournamentSystemAnalyzer.csproj`
- 空ルールの要求ファイル入力スモーク
- 手動入力保存のスモーク

## 今回は触らないこと

- `RequestFileCreate` 本体の移動
- `ManualInputRecordingSessionStarter` の移動
- namespace の大規模変更
- 要求ファイルの入出力書式