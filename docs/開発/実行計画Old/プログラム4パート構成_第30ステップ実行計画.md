# プログラム4パート構成 第30ステップ実行計画

## 目的

ライフサイクル上の `AfterRequestFileCheck` フォルダーへ、要求ファイルチェック後の入力適用処理を配置する。

第30ステップでは、チェック済み要求ファイルの内容を `Console.In` に流し込む `RequestInputApplier` を `Application/Execution/AfterRequestFileCheck/` に移す。

## 対象

- `ShogiTournamentSystemAnalyzer/Application/Input/RequestInputApplier.cs`
- `ShogiTournamentSystemAnalyzer/Application/Execution/AfterRequestFileCheck/`

## 作業

1. 配置マップを更新する。
2. `RequestInputApplier.cs` を `Application/Execution/AfterRequestFileCheck/` へ移動する。
3. namespace は今回は維持し、外部挙動を変えない。

## 確認

- `dotnet build .\ShogiTournamentSystemAnalyzer\ShogiTournamentSystemAnalyzer.csproj`
- 空ルールの要求ファイル入力スモーク
- 手動入力保存のスモーク

## 今回は触らないこと

- `InputSourceConfiguration` の移動
- 要求ファイルチェック本体の追加変更
- namespace の大規模変更
- 要求ファイルの入出力書式