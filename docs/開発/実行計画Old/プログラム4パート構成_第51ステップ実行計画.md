# プログラム4パート構成 第51ステップ実行計画

## 目的

分析側の旧分類 namespace のうち、Simulation ドメインをフォルダー構成に合わせる。

`Application.Modes` や `Application.TournamentFramework` は複数の移動前分類を表していたため、第51ステップでは Simulation ドメインだけを対象にして、TournamentQualityEvaluator 側は次ステップに分ける。

## 対象

- `Application/Analysis/Domains/Simulation/Modes/`
- `Application/Analysis/Domains/Simulation/SimulationContext/`
- `Application/Analysis/Domains/Simulation/SimulationMainline/`
- `Application/Analysis/Domains/Simulation/TournamentFramework/`
- `Application/Analysis/Domains/Simulation/UseCases/`

## 作業

1. Simulation の Modes / Context / Mainline / TournamentFramework / UseCases namespace をフォルダー構成へ合わせる。
2. Simulation ドメインを参照する `using` を新 namespace へ更新する。
3. TournamentQualityEvaluator 側の `Application.Modes` は今回は触らない。

## 確認

- `dotnet build .\ShogiTournamentSystemAnalyzer\ShogiTournamentSystemAnalyzer.csproj`
- 空ルールの要求ファイル入力スモーク
- 手動入力保存のスモーク

## 今回は触らないこと

- TournamentQualityEvaluator 側 namespace の変更
- 境界側 namespace の全面変更
- 分析ロジックの変更