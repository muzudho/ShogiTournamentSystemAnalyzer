# プログラム4パート構成 第53ステップ実行計画

## 目的

分析境界側に残っている旧分類 namespace を、`Analysis.Boundaries` 配下へ合わせる。

`BoundaryDataBuilders` と `ReportOutputPathBuilder` は partial class のため、ファイル配置は境界別でも namespace は境界共通の `Application.Analysis.Boundaries` にまとめる。

## 対象

- `Application.Helpers`
- `Application.Paths`
- `Application.Validation`
- `Application.DataDefinitions`
- `Application/Analysis/Boundaries/`

## 作業

1. `BoundaryDataBuilders` を `Application.Analysis.Boundaries` namespace へ変更する。
2. `ReportOutputPathBuilder` を `Application.Analysis.Boundaries` namespace へ変更する。
3. `ModeSupportHelpers` と `FinalStageValidators` を Request 境界 namespace へ変更する。
4. FinalRanking の DataDefinitions を FinalRanking 境界 namespace へ変更する。
5. 参照元の `using` を新 namespace へ更新する。

## 確認

- `dotnet build .\ShogiTournamentSystemAnalyzer\ShogiTournamentSystemAnalyzer.csproj`
- 空ルールの要求ファイル入力スモーク
- 手動入力保存のスモーク

## 今回は触らないこと

- 境界クラスの分割粒度変更
- 出力パス仕様の変更
- テーブル定義 JSON の仕様変更