# プログラム4パート構成 第35ステップ実行計画

## 目的

手動入力を記録する TextReader を、ライフサイクル上の `AfterManualInput` フォルダーへ配置する。

`ManualInputRecordingTextReader` は、手動入力後に記録セッションを開始したあと、分析中の入力を記録し続けるための部品である。そのため `ManualInputRecordingSessionStarter` と同じ `Application/Execution/AfterManualInput/` に置く。

## 対象

- `ShogiTournamentSystemAnalyzer/Application/Input/ManualInputRecordingTextReader.cs`
- `ShogiTournamentSystemAnalyzer/Application/Execution/AfterManualInput/`

## 作業

1. 配置マップを更新する。
2. `ManualInputRecordingTextReader.cs` を `Application/Execution/AfterManualInput/` へ移動する。
3. namespace は今回は維持し、外部挙動を変えない。
4. `#メモ：` や `#ここは：` で始まるユーザー用メモは触らない。

## 確認

- `dotnet build .\ShogiTournamentSystemAnalyzer\ShogiTournamentSystemAnalyzer.csproj`
- 空ルールの要求ファイル入力スモーク
- 手動入力保存のスモーク

## 今回は触らないこと

- `InputSourceConfiguration` の移動
- `RequestInputSession` の移動
- namespace の大規模変更
- 要求ファイルの入出力書式