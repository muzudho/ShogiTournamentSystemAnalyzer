# プログラム4パート構成 第44ステップ実行計画

## 目的

`Application/Helpers/BoundaryDataBuilders.cs` にまとまっていた境界データ組み立て処理を、4 大境界のフォルダーへ分けて配置する。

ファイル単位で境界が見えるようにし、`Analysis/Boundaries/` の進捗をフォルダーから読み取りやすくする。

## 対象

- `Application/Helpers/BoundaryDataBuilders.cs`
- `Application/Execution/Analysis/Boundaries/Request/RequestBoundaryDataBuilders.cs`
- `Application/Execution/Analysis/Boundaries/TournamentFinalState/TournamentFinalStateBoundaryDataBuilders.cs`
- `Application/Execution/Analysis/Boundaries/FinalRanking/FinalRankingBoundaryDataBuilders.cs`
- `Application/Execution/Analysis/Boundaries/TournamentQualityReport/TournamentQualityReportBoundaryDataBuilders.cs`

## 作業

1. `BoundaryDataBuilders` を `partial` class にする。
2. 要求境界、大会最終状態境界、最終順位境界、大会品質レポート境界の単位でファイルを分ける。
3. namespace は今回は維持し、呼び出し側の挙動を変えない。
4. 元の `Application/Helpers/BoundaryDataBuilders.cs` は削除する。

## 確認

- `dotnet build .\ShogiTournamentSystemAnalyzer\ShogiTournamentSystemAnalyzer.csproj`
- 空ルールの要求ファイル入力スモーク
- 手動入力保存のスモーク

## 今回は触らないこと

- namespace の大規模変更
- 境界データの内容変更
- 出力ファイル仕様の変更