# プログラム4パート構成 第7ステップ実行計画

## 目的

第6ステップで STSA の section parse を `StsaInputSectionParser` に分離した。
第7ステップでは、STSA の文字列値を既存 enum / メニュー番号へ解釈する処理を `StsaInputValueParser` に分離する。

## 現状

`RequestInputFileReader` の末尾には、次の値 parse が残っている。

- `AnalysisFlowMode`
- `RuleProfileMode`
- `Off` / `On`
- `Good` / `Bad`
- `TournamentRuleSetMode`

これらは入力列の組み立て処理から独立しているため、別クラスにできる。

## 今回やること

1. `Application/Input/StsaInputValueParser.cs` を追加する。
2. `ParseAnalysisFlowMode` などの値 parse メソッドを移す。
3. `RequestInputFileReader` は `using static` で value parser を使う。
4. 値の解釈ルールやエラーメッセージは変えない。

## 今回やらないこと

- 値 parse の仕様変更
- enum 変換の共通化
- STSAInput/2 と STSAInput/3 の converter 分割
- テスト追加

## 完了条件

- STSA 値 parse が `StsaInputValueParser` に分離されている。
- `RequestInputFileReader` から値 parse メソッドが消えている。
- `dotnet build` が成功する。
- `--input-file` 経由の空ルール smoke 実行が成功する。