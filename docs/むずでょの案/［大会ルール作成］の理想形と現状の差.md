# ［大会ルール作成］の理想形と現状の差

今日はここまでにするとき用に、現状整理を短く残しておくぜ（＾▽＾）

## いまの理解

最終的にやりたいのは、次の流れだ。

- ［大会ルール］を作成、またはファイルから読込
- ［プレイヤー一覧］を作成、またはファイルから読込
- ［順位付け設定］を作成、またはファイルから読込
- ［シミュレーション］開始
- ［大会結果］出力
- ［最終順位付け］開始
- ［最終順位］出力
- ［大会品質評価］開始
- ［大会品質レポート］出力

ただし、今の実装はまだそこまで完全分離されていない。
モードごとに、入力・実行・順位・出力が縦にまとまっている。

## 今の実装に近い図

```mermaid
flowchart TD
	A[起動] --> B[入力方法選択]
	B --> C[実行入口選択<br/>対局シミュレーション / 品質評価]
	C --> D[対象ルール選択<br/>通常 / 本戦 / 大会進行フレームワーク / 空ルール]

	D --> E1[通常ルール入力]
	D --> E2[本戦ルール入力]
	D --> E3[大会進行フレームワーク入力]
	D --> E4[空ルール入力]
	C -->|品質評価| Q1[品質評価入力]

	E1 --> S1[通常ルール実行]
	E2 --> S2[本戦ルール実行]
	E3 --> S3[大会進行フレームワーク実行]
	E4 --> S4[空ルール実行]

	S1 --> R1[最終順位計算]
	S2 --> R2[最終順位計算]
	S3 --> R3[代表実行 + aggregate 集計]
	S4 --> R4[空結果]

	R1 --> O1[結果CSV / Markdown]
	R2 --> O2[結果CSV / Markdown]
	R3 --> O3[aggregate順位表]
	R3 --> O4[representative順位表]
	R3 --> O5[representative大会結果]
	R4 --> O6[空ルール結果 + 空大会結果]

	Q1 --> Q2[品質評価実行]
	Q2 --> Q3[品質評価レポート]
```

## 理想形の図

```mermaid
flowchart TD
	A[大会ルール<br/>作成 / 読込] --> X[シミュレーション実行]
	B[プレイヤー一覧<br/>作成 / 読込] --> X
	C[順位付け設定<br/>作成 / 読込] --> X

	X --> D[大会進行]
	D --> E[大会結果]
	E --> F[最終順位付け]
	F --> G[最終順位]

	E --> H[大会品質評価]
	G --> H
	H --> I[大会品質レポート]
```

## 今、くっついているところ

### 1. 入力とモードがくっついている

今は、

- 通常ルール
- 本戦ルール
- 大会進行フレームワーク
- 空ルール

ごとに入力手順が強く分かれている。

つまり、

- ［大会ルール］
- ［プレイヤー一覧］
- ［順位付け設定］

を独立した部品として組み立てるというより、モード別の入力画面としてまとまっている。

### 2. 大会進行と順位付けがくっついている

特に大会進行フレームワークでは、実行処理の中で

- 大会進行
- representative の順位付け
- aggregate の順位付け

をまとめて扱っている。

将来的には、

- 大会結果を作る処理
- 順位付けする処理

をもっと分けたい。

### 3. representative と aggregate がくっついている

今は同じ実行の流れの中で、

- representative 順位表
- aggregate 順位表
- representative 大会結果

をまとめて出している。

これは見やすくはなってきたが、概念としてはまだ分離途中だ。

### 4. 品質評価はまだ別系統

品質評価は、

- シミュレーション結果を利用する

という意味ではつながっているが、実装としてはまだ別の入口・別の実行系としてまとまっている。

将来的には、

- 大会結果
- 最終順位
- 品質評価

が自然につながる形へ整理したい。

## 一言でまとめると

今は、

- モードごとに縦にまとまった実装

で、
最終的には、

- 大会ルール
- プレイヤー一覧
- 順位付け設定
- 大会進行
- 大会結果
- 最終順位
- 品質評価

を横に分離したい、という段階だ。

## 次に見たい観点

次に整理するとよさそうなのは、次のどれかだ。

- ファイル対応つきの Mermaid 図
- 入力部品 / 実行部品 / 出力部品の分離図
- representative と aggregate の責務分離図

## 完成図に近い設計骨子

完成図に近い考え方として、次の 6 つをデータ境界として扱う。

- ◆大会ルールデータ
- ◆プレイヤー一覧データ
- ◆順位付けの設定データ
- ◆大会結果データ
- ◆最終順位データ
- ◆大会品質レポート

このとき、処理フローは次の 3 本へ分ける。

### 1. シミュレーション・フロー

- 入力: ◆大会ルールデータ、◆プレイヤー一覧データ
- 出力: ◆大会結果データ

### 2. 順位付け・フロー

- 入力: ◆順位付けの設定データ、◆大会結果データ
- 出力: ◆最終順位データ

中間順位付けも最終順位付けも、同じフローを使い、違うのは渡す大会結果データの時点だけ、とみなせる。

### 3. 大会品質評価・フロー

- 入力: ◆最終順位データ
- 必要に応じて補助入力: ◆プレイヤー一覧データ、◆大会ルールデータ
- 出力: ◆大会品質レポート

## データ境界ベースの完成図

```mermaid
flowchart TD
	A[◆大会ルールデータ] --> S[シミュレーション・フロー]
	B[◆プレイヤー一覧データ] --> S
	S --> C[◆大会結果データ]

	D[◆順位付けの設定データ] --> R[順位付け・フロー]
	C --> R
	R --> E[◆最終順位データ]

	E --> Q[大会品質評価・フロー]
	B --> Q
	A --> Q
	Q --> F[◆大会品質レポート]
```

## 中間移行の図

現状からいきなり完全分離するのではなく、まずは `大会結果データ -> 最終順位データ` を独立させるのがよさそうだ。

```mermaid
flowchart LR
	A[入力とモード選択] --> B[シミュレーション実行]
	B --> C[◆大会結果データ]
	C --> D[順位付け・フロー]
	D --> E[◆最終順位データ]
	E --> F[品質評価・フロー]
```

## ファイル対応つきの現状イメージ

```mermaid
flowchart TD
	P[Program.cs<br/>実行入口とモード分岐] --> M[Simulation*Mode*.cs<br/>モード別入力と実行入口]
	M --> T[SimulationTournamentFrameworkModeExecution.cs<br/>大会進行 + representative順位 + aggregate順位 + 出力]
	T --> O[ResultCsvWriter.cs<br/>CSV / Markdown 出力]
	P --> Q[QualityEvaluation*.cs<br/>品質評価の別入口]
```

## 現状との差分をもう一度まとめる

### 1. モード別入力の縦結合

今は入力がモード単位でまとまっていて、

- 大会ルール
- プレイヤー一覧
- 順位付け設定

が独立の部品というより、モード固有の入力列として扱われている。

### 2. `大会結果データ -> 最終順位データ` が独立していない

今は特に `SimulationTournamentFrameworkModeExecution.cs` の中で、

- 大会結果の生成
- representative 順位
- aggregate 順位
- 出力

が一体になっている。

ここを切り出せると、設計がかなりすっきりする。

### 3. 品質評価がまだ正式に `最終順位データ` を受けていない

今の品質評価は別入口・別実装系にまとまっている。

将来的には、

- 最終順位データ
- 必要ならプレイヤー一覧データ
- 必要なら大会ルールデータ

を受けて品質レポートを出す形に寄せたい。

## 次の分離の優先順

1. `大会結果データ` をシミュレーションの唯一の成果物として扱う
2. `順位付けの設定データ + 大会結果データ -> 最終順位データ` を独立フローにする
3. 品質評価を `最終順位データ` ベースへ寄せる
4. 最後に入力部品をモード別 UI から切り離す

おやすみだぜ（＾▽＾）
