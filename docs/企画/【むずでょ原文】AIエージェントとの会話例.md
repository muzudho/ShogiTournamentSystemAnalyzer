# 【むずでょ原文】AIエージェントとの会話例

```plaintext
Codex さん、こんばんわだぜ（＾▽＾）
```

```plaintext
後は何が残ってるんだぜ（＾～＾）？
```

```plaintext
それ、メモしてくれだぜ（＾～＾）
```

```plaintext
サンキューだぜ～（＾▽＾）！
続きは新しいスレッドでやろうぜ（＾～＾）！

大仕事、サンキューだぜ～（＾▽＾）！
続きは新しいスレッドでやろうぜ（＾～＾）！

おー、素晴らしいぜ、サンキューだぜ～（＾▽＾）！
続きは新しいスレッドでやろうぜ（＾～＾）！

```

```plaintext
続きは `docs/続きはここから.md` に書いてるから読んでくれだぜ（＾▽＾）
じゃあ、コーディングとビルドを開始してくれだぜ（＾▽＾）！
```

```plaintext
続きを頼むぜ～（＾▽＾）！
```

```plaintext
サンキューだぜ～（＾▽＾）！
今日はもう遅いからここまでにしようぜ（＾～＾）
お休みだぜ～（＾▽＾）！
```


## 内容

```plaintext
お前がいじったファイルの行の末尾の改行コードが
Windows と Linux の混合になってるから、
Windows 向けに揃えてくれだぜ（＾～＾）
```

```plaintext
そのドキュメント、日付が付いていないから内容が古くなっている可能性があるぜ（＾～＾）！
内容が合っているか調査して、内容が古くなっているなら、日付を付けて更新してほしいぜ（＾～＾）
```


## その他

```plaintext
［要求ファイル］に次のように書いてるだろ（＾～＾）

`#[Section] SimulationStep.RuleProfileAttributes`

これって、以下みたいな `カテゴリー[インスタンス]` 表記（配列表記）を許容したら読みやすくならないかだぜ（＾～＾）？
どう思う（＾～＾）？

`#[Section] Step["Simulation"].RuleProfileAttributes`
```

```plaintext
正規表現の記述が大変になりそうだな（＾～＾）
じゃあ単純に、 `Step.A.B` を `AStep.B` にするような仕様変更だったらどうだぜ（＾～＾）？
```

```plaintext
良い案だと思うぜ（＾～＾）！
じゃあ、`Step.` 階層を `Step` を接尾辞に仕様変更する案で、まず実装計画を立ててくれだぜ（＾▽＾）！
```

```plaintext
古い section 名は廃止して、 `*Step` にしたぜ（＾～＾）
古いドキュメントが残っているのかだぜ（＾～＾）？
```

```plaintext
じゃあ、古い section 名はドキュメントからも、実装上も廃止しようぜ（＾～＾）
古いドキュメントから消して前後の文脈がおかしくなるときは、 `（削除）` とでもプレースホルダーを置いておけばいいだろ（＾～＾）
```

```plaintext
AnalysisFlowSelection クラスにはまだ Steps 配列があるみたいなんだが、これは必要なのかだぜ（＾～＾）？
使ってないなら、消してもいいんじゃないかだぜ（＾～＾）？

Step って、［シミュレーション域］、［最終順位付け域］、［大会品質評価域］の３つしかないんじゃないのかだぜ（＾～＾）？
他にも Step があるのかだぜ（＾～＾）？

Step を配列にするんじゃなくて、
Step をプロパティにして、［シミュレーション域］、［最終順位付け域］、［大会品質評価域］の３つのプロパティに分けてくれないかだぜ（＾～＾）？
```

```plaintext
PowerShell でテキストファイルを作成するたび、UTF-8 の BOM 付きで作ってるよな（＾～＾）
PowerShell の設定で UTF-8 は BOM なしになるようにできないのかだぜ（＾～＾）？

PowerShell 5.1 をアンインストールして、PowerShell の最新版をインストールしてくれだぜ（＾～＾）
```

```plaintext
フォルダー構成はすっきりしたかな（＾～＾）？　どう思う（＾～＾）？
```

```plaintext
リポジトリーのルートに `Volume` フォルダーを作ったんだぜ（＾▽＾）
ここは、過去ログを置いていこうと思うんだぜ（＾～＾）
どんどん成長していくと思うから、過去ログを置いていく場所が必要だと思ったんだぜ（＾～＾）！
どう思う（＾～＾）？

じゃあ Archives フォルダーにするぜ（＾▽＾）！
ここに docs/開発/実行計画Old とか、過去ログ関連を移動して、
ドキュメントにリンクがあれば貼りなおしてくれだぜ（＾▽＾）

現状、ツールとしてよくできてきたと思うんだぜ（＾～＾）
１回試しに、大会ルールを考えて［要求ファイル］を作って、その品質を調べてみてくれだぜ（＾▽＾）
プレイヤーとか、レギュレーションを決めておくといいと思うぜ（＾～＾）

要求ファイルの改行コードが混合になってるな（＾～＾）
改行コードが混合の［要求ファイル］は弾くか、受け入れるか、
どっちがいいと思う（＾～＾）？

じゃあ、連続する改行は無視する、という仕様を明示しておくかだぜ（＾～＾）？

今、品質評価サマリーレポートには総合的な指標がないだろ（＾～＾）
品質評価サマリーレポートに［総合点］を追加する案はどうだぜ（＾～＾）？
ビデオゲームみたいに、［総合点］を最高にするのを目指すんだぜ（＾～＾）
10000点とか、100000点とか、そういう感じで（＾～＾）！
［総合点］をどう計算するかも、［要求ルール］で指定できるといいと思うんだぜ（＾～＾）
どう思う（＾～＾）？

PowerShell の [ ] がワイルドカード扱いされているのでよく -LiteralPath を使ってるよな（＾～＾）
なんどもつまづいているようなんで、メモっておいてくれだぜ（＾～＾）
```
