# 入力ファイル仕様

## 概要
このアプリは `--request-file` を使って、対話入力の代わりにテキストファイルから入力できます。

値だけを並べると意味が分かりにくくなりやすいため、現在の入力ファイルでは `#[Prompt]` と `#[Enter]` を使う運用を想定しています。

次世代方式として、`#[Format] STSAInput/2` と `#[Section] ... #[EndSection]` を使うセクション型プロトコルを検討してきました。  
さらに、標準ルール / 本戦ルールを 1 本で書ける統一案として `STSAInput/3`、分析フローを直列ステップで表す方式として `STSAInput/4` も追加で整理しています。

- 仕様ドラフト: [STSAInput/2 仕様](./STSAInput2仕様.md)
- 仕様ドラフト: [STSAInput/3 仕様](./STSAInput3仕様.md)
- 仕様ドラフト: [STSAInput/4 仕様](./STSAInput4仕様.md)

## 置き場所
実行用入力ファイルは、`Inputs` 配下に置く運用を基本にします。ファイル名は、要求ファイルだと分かるように `*.request.txt` にします。手動入力から保存する場合も、デフォルト名と明示ファイル名は `*.request.txt` へ正規化します。

- `Inputs/Smoke`
  - 1 回や少数回のスモークテスト用
- `Inputs/Bench`
  - 単発評価や軽いベンチマーク用
- `Inputs/Sweeps`
  - n% スイープ実験用

関連フォルダー:

- `ShogiTournamentSystemAnalyzer/Examples`
	- 人が読むための見本、PlayerSymbols、メモ
	- 詳細: `ShogiTournamentSystemAnalyzer/Examples/README.md`
- `Data/Players`
  - 再利用する選手データの置き場
- `Data/Matches`
  - 再利用する対局表、参考対局表の置き場
- `Data/FinalStage`
  - 本戦専用モードのグループ対応表や補助CSVの置き場
- `Data/RuleSets`
  - 保存して再利用する大会ルールの置き場
- `Output`
  - 実行で自動生成される CSV / Markdown の保存先
  - `Ranking/FinalRanking`: 通常結果、本戦結果、aggregate 結果
  - `Simulation/TournamentFinalState`: 大会最終状態、代表実行の対局記録
  - `TournamentQualityEvaluator/TournamentQualityReport`: 品質評価の Summary / Players / Sweeps

## 実行方法
```powershell
dotnet run --project .\ShogiTournamentSystemAnalyzer\ShogiTournamentSystemAnalyzer.csproj -- --request-file .\Inputs\Bench\quality_input_[先手8x後手8]_[Neutral_Single10]_[STSAInput2_draft].request.txt
```

## コメントと終端記法
入力ファイルでは `#` で始まる行をコメントとして無視できます。

- `#[Prompt]`
  - その直後の値が何を表すかを書くための注記です
- `#[Enter]`
  - 空行 1 行として扱われます

`STSAInput/2` 以降のセクション型要求ファイルでは、通常の空行および連続する空行は読み飛ばします。空行はセクションの開始・終了・CSV入力終了を表しません。セクションの範囲は `#[Section]` と `#[EndSection]` だけで決まります。

この仕組みにより、空行の意味が分かりにくい問題を避けつつ、対話入力の流れをそのままファイルに写せます。

### 例
```plaintext
#[Prompt] モード番号を入力してください [1]
3
#[Prompt] 同Elo対局時の先手勝率(%)を入力してください [51]
51

#[Prompt] 選手 / Player 一覧CSVを貼り付けてください。入力終了は #[Enter] です。
name,elo
Alice,1500
Bob,1480
#[Enter]
```

## 基本ルール
- `#` で始まる行は無視されます
- `#[Enter]` は空行として扱われます

## 今後の入力プロトコル案

現行方式は、対話画面の流れをそのまま入力ファイルへ写しやすい反面、プロンプト文言や入力順の変更に影響されやすい面があります。

そのため、将来的には UI 文言から独立した安定キー中心の方式として、`STSAInput/2`、その発展案である `STSAInput/3`、直列フロー対応の `STSAInput/4` を検討しています。

- `#[Format] STSAInput/2`
- `#[Section] Meta`
- `FirstPlayerWinRatePercent=51`
- `#[Section] PlayersCsv`
- `#[Section] MatchesInput`
- `#[EndSection]`

詳細は [STSAInput/2 仕様](./STSAInput2仕様.md)、[STSAInput/3 仕様](./STSAInput3仕様.md)、[STSAInput/4 仕様](./STSAInput4仕様.md) を参照してください。
- 実際の空行も従来どおり入力終了に使えます
- 入力順はモードごとに異なります
- 詳細な入力順は [モード別ガイド](./モード別ガイド.md) を参照してください
- 入力値の誤りが続くときは、主要な入力は 10 回で中断します

## 入力失敗時の扱い
- 選択入力、数値入力、CSV入力などの主要な入力は、誤りが 10 回続くと中断します
- 中断時は、最後に失敗した入力内容の種別とエラー理由を表示します
- 入力ファイル実行でも同じ扱いです

## 向いている使い方
- 再現可能な実験の保存
- ベンチマークやスモークテストの定型化
- 品質評価の実行手順共有
- 数値入力に説明を添えた保守しやすい実験ファイルの作成

## STSAInput/2 の空ルールサンプル
空ルールでは、`RuleProfileMode=Empty` を指定します。

```plaintext
#[Format] STSAInput/2

#[Prompt] EmptyRuleMinimal
#[Section] Meta
AnalysisFlowMode=Simulation
RuleProfileMode=Empty
OutputPath=Output\Ranking\FinalRanking\empty_rule_final_ranking_[最小].csv
#[EndSection]

#[Section] Output
OutputPath=Output\Ranking\FinalRanking\empty_rule_final_ranking_[最小].csv
#[EndSection]
```

品質評価の既定出力先は、用途ごとに次へ分かれます。

- `Output/TournamentQualityEvaluator/TournamentQualityReport/Summary`
- `Output/TournamentQualityEvaluator/TournamentQualityReport/Players`
- `Output/TournamentQualityEvaluator/TournamentQualityReport/Sweeps`

## 関連資料
- [モード別ガイド](./モード別ガイド.md)
- [CSVと出力](./CSVと出力.md)
- [品質評価](./品質評価.md)


