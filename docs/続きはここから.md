# 続きはここから

作業再開時に最初に見る短いメモです。詳しい仕様は別ファイルへ分けています。

## いまの状態

- 第63ステップとして、分析フローを直列選択へ変更済み。
- コンソールでは「シミュレーションをしますか？」の後に「品質評価をしますか？」を聞く。
- `RequestBoundary` は `AnalysisFlowSelection` を持つ。
- 要求ファイルは `STSAInput/4` と `AnalysisFlowSteps` に対応済み。
- `STSAInput/2` / `STSAInput/3` は `AnalysisFlowMode` 互換として読み取り継続。
- 代表 smoke 入力は `STSAInput/4` へモダナイズ済み。
- 生成される依頼ログも `#[Format] STSAInput/4` / `AnalysisFlowSteps=QualityEvaluation` で出る。
- 第65ステップとして、`STSAInput/4` の `Simulation + Standard + 20局以下` は `AnalysisRequest` へ直接パースして実行する新ルートを追加済み。
- 第66ステップとして、`STSAInput/4` の `QualityEvaluation + Standard` は `StandardQualityEvaluationRequest` へ直接パースして実行する新ルートを追加済み。
- 第67ステップとして、`STSAInput/4` の `QualityEvaluation + FinalStage` は `FinalStageQualityEvaluationRequest` へ直接パースして実行する新ルートを追加済み。
- 第68ステップとして、`STSAInput/4` の `Simulation + FinalStage + 20局以下` は `FinalStageSimulationRequest` へ直接パースして実行する新ルートを追加済み。
- 第69ステップとして、`STSAInput/4` の `Simulation + FinalStage + 20局超 + SimulationCount指定あり` は `FinalStageSimulationRequest` へ直接パースして実行する新ルートへ移行済み。
- 標準品質評価と本戦品質評価の新ルートは `ExecutionMode=Single` と `ExecutionMode=Sweep` に対応する。
- 標準シミュレーションの新ルートは、20局以下の厳密計算に対応する。
- 本戦シミュレーションの新ルートは、20局以下の厳密計算と、20局超の `SimulationCount` 指定あり近似計算に対応する。
- 20局超の品質評価は、`SimulationCount` が指定されている場合だけ新ルートで実行する。
- 新ルートでは `InputFromSomewhere.UseText(...)` による対話入力偽装を使わず、`AnalysisRequestDispatcher` から各 mainline のオブジェクト受け取り口を呼ぶ。
- 新ルート未対応の要求ファイルは、従来どおり legacy 入力列へ変換して fallback する。

## 今回追加した計画と仕様

- [第63ステップ実行計画](./開発/step63-flow-sequentialization-plan.md)
- [第64ステップ実装計画](./開発/プログラム4パート構成_第64ステップ実行計画.md)
- [STSAInput/4 仕様](./設計/STSAInput4仕様.md)
- [第65ステップ実行計画](./開発/プログラム4パート構成_第65ステップ実行計画.md)
- [第66ステップ実行計画](./開発/プログラム4パート構成_第66ステップ実行計画.md)
- [第67ステップ実行計画](./開発/プログラム4パート構成_第67ステップ実行計画.md)
- [第68ステップ実行計画](./開発/プログラム4パート構成_第68ステップ実行計画.md)
- [第69ステップ実行計画](./開発/プログラム4パート構成_第69ステップ実行計画.md)

## 検証

- `dotnet build .\ShogiTournamentSystemAnalyzer\ShogiTournamentSystemAnalyzer.csproj` は成功。
- `git diff --check` は成功。
- 次の smoke は成功。
  - `Inputs/Smoke/simulation_input_[先手1x後手1]_[Neutral_Exact]_[STSAInput4_smoke].txt`
  - `Inputs/Smoke/quality_input_[先手8x後手8]_[Neutral_Single10]_[STSAInput4_smoke].txt`
  - `Inputs/Smoke/quality_sweep_input_[先手8x後手8]_[Twill_50to100_smoke10].txt`
  - `Inputs/Smoke/quality_input_[トップ集団大きめ]_[FinalStage_Single10]_[STSAInput4_smoke].txt`
  - `Inputs/Smoke/simulation_input_[本戦16]_[FinalStage_Exact]_[STSAInput4_smoke].txt`
  - `Inputs/Smoke/simulation_input_[本戦16]_[FinalStage_Simulation10]_[STSAInput4_smoke].txt`

## 注意点

- `RequestFileCheckWorkflow` は raw `RequestText` を返す。`ApplicationWorkflow` 側で、まず `StsaInput4RequestParser.TryParse` を試し、未対応時だけ legacy converter を呼ぶ。
- `apply_patch` が `writing outside of the project` で失敗するときは、[Codex apply_patch 回避メモ](./開発/Codex_apply_patch回避メモ_20260605.md) の PowerShell 手順を使う。
- `STSAInput/4` の `AnalysisFlowSteps=Simulation,QualityEvaluation` は形式として予約済みだが、要求ファイルからの自動実行はまだ未対応。
- 複数ステップを1ファイルで完全自動実行するには、ステップ別入力セクションの仕様追加が必要。
- `RuleProfileMode=FinalStage` のシミュレーションは、20局以下なら新ルートで動く。20局超は `SimulationCount` がある場合だけ新ルートで動き、未指定なら legacy fallback で動く。
- smoke 実行で `Output` 配下に CSV / Markdown / `.stsa.txt` が生成・更新される。
- smoke 実行で生成された第69ステップ用の `Output/FinalRanking` CSV / Markdown は、今回の実装差分から外すため削除済み。
- staging は触らない。作業後に `git diff --cached --stat` が空であることを確認する。

## 次にやること

- `git status --short` で差分を確認する。
- 第69ステップの実装差分をレビューする。
- 第70ステップ候補を選ぶ。
  - 候補A: `Simulation + Standard + 20局超 + SimulationCount指定あり` を新ルートへ移す。
  - 候補B: `AnalysisFlowSteps=Simulation,QualityEvaluation` の複数ステップ要求仕様を追加する。

## 参照

- [docs フォルダー案内](./README.md)
- [設計 README](./設計/README.md)
- [開発 README](./開発/README.md)